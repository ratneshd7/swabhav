let msg: String=`hello
                 world`,
    salary:Number=5000.50,
    isMale:boolean=true,
    age:Number=20
    let formatedString:String=`msg is:${msg}
                        Salary is:${salary}
                        ismale is:${isMale}
                        age is:${age}`

                        console.log(formatedString)

